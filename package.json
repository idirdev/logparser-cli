{
  "name": "@idirdev/logparser-cli",
  "version": "1.0.0",
  "description": "A versatile CLI tool for parsing, filtering, and analyzing log files from multiple formats",
  "author": "idirdev",
  "license": "MIT",
  "bin": {
    "logparser": "./dist/index.js"
  },
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "ts-node src/index.ts",
    "start": "node dist/index.js",
    "clean": "rm -rf dist",
    "prepublishOnly": "npm run build",
    "test": "vitest run"
  },
  "keywords": ["log", "parser", "cli", "analytics", "filter", "tail", "nginx", "apache"],
  "dependencies": {
    "chalk": "^4.1.2",
    "commander": "^12.1.0"
  },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.5.0",
    "vitest": "^2.0.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
